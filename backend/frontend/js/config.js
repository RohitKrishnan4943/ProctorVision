// config.js — FINAL (FastAPI same-origin setup)

// Backend API base URL
// Frontend is served by FastAPI, so same-origin is used
window.API_BASE_URL = "/api";

// Debug log
console.log("✅ Using backend:", window.API_BASE_URL);
